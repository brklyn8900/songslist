<h1>
  <%= @song.title %>
</h1>
<small><%= link_to 'Edit', edit_song_path(params[:id]) %></small>
<small><%= link_to 'Delete',  song_path(params[:id]), method: :delete, data: {confirm: "Are you sure you want to do that?"} %></small>
<p>
  <%= @song.artist %>
</p>
<p>
  <%= link_to @song.url, @song.url %>
</p>
